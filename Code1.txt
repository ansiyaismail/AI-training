If your task is simply "Build a classification model and train it", Random Forest is a strong choice:


from sklearn.ensemble import RandomForestClassifier

# Build model
model = RandomForestClassifier(
    n_estimators=100,
    random_state=42
)

# Train model
model.fit(X_train, y_train)

print("Model training completed.")
